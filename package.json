{
  "name": "@filearchitect/monorepo",
  "private": true,
  "version": "0.1.8",
  "description": "File Architect monorepo",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run --config ./vitest.config.ts",
    "test:watch": "vitest --config ./vitest.config.ts",
    "demo": "pnpm --filter browser-demo dev",
    "dev": "pnpm --parallel -r dev",
    "cli": "pnpm --filter @filearchitect/cli dev",
    "clean": "pnpm -r exec rm -rf dist node_modules",
    "patch": "git add . && git commit -m \"chore: prepare for version bump\" || true && npm --no-git-tag-version version patch && pnpm -r exec npm version $(npm pkg get version | sed 's/\"//g') --no-git-tag-version && git add . && git commit -m \"chore: version bump to $(npm pkg get version | sed 's/\"//g')\" && git tag -f v$(npm pkg get version | sed 's/\"//g') && git push && git push --tags -f && pnpm build && pnpm publish -r --filter @filearchitect/core --filter @filearchitect/cli",
    "publish": "pnpm build && pnpm publish -r --filter @filearchitect/core --filter @filearchitect/cli",
    "try": "pnpm --filter @filearchitect/cli dev create structure.txt output --replace-file main:bar --replace-folder inal:demo && open packages/cli/output"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^1.6.0"
  }
}
